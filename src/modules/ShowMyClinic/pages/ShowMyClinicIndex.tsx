import { Stack } from "@mui/material";
import MyClinicCard from "../components/ShowMyClinicIndex/ShowMyClinicCard";
const ShowMyClinicIndex = () => {
  return (
    <Stack sx={{ px: "26px", borderRadius: 6 }}>
      <MyClinicCard />
    </Stack>
  );
};

export default ShowMyClinicIndex;
